// The util contains showing exit confirmation and exiting app features
const showExitApp = () => {
  const element = window.document.querySelector('.app-exit-confirmation');
  if (element) {
    element.click();
  }
};
export default showExitApp;

// VIZIO exit app function
